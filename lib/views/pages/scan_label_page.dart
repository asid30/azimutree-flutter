// Scan feature removed. This page was deprecated and its functionality
// (camera + OCR via ML Kit) has been removed from the project.
// The file remains as a placeholder to avoid accidental imports elsewhere.

import 'package:flutter/material.dart';

class ScanLabelPage extends StatelessWidget {
  const ScanLabelPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Scan Kode Label')),
      body: const Center(child: Text('Fitur Scan Kode Label telah dihapus.')),
    );
  }
}
